# Copy to .env and load in your shell, or set in the environment before running `avi`.
# NCBI E-utilities: https://www.ncbi.nlm.nih.gov/books/NBK25497/

ENTREZ_EMAIL=you@example.com
# NCBI_API_KEY=your_key_here

# Optional: cache AlphaFold / E-utilities GET responses on disk (requires: pip install ".[cache]")
# AVI_USE_HTTP_CACHE=1
